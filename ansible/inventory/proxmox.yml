proxmox:
  hosts:
    csgo.home:
    containers.home:
    vpn.home:
  vars:
    ansible_user: luigi
    ansible_become: true

dockerhost:
  hosts:
    containers.home:
  vars:
    ansible_user: luigi
    ansible_become: true

dockeragent:
  hosts:
    csgo.home:
  vars:
    ansible_user: luigi
    ansible_become: true